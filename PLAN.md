# BMG 工作区真正隐藏与登录恢复实施计划

日期：2026-09-20。接手实现：5.6 sol。项目：`C:\Users\Songjx\Documents\ChatGPT\browser-mcp-gateway`。

本文最初是实施交接计划；2026-09-20 已由 5.6 sol 按本计划完成代码实施。原计划内容继续保留作为设计依据；`25b2b51` 包含正式接入前的独立隐藏试验脚本和结果说明。

实施结果摘要：正式工作区已从 `(-32000,-32000)` 离屏迁移到 `ShowWindowAsync(SW_HIDE)` 真隐藏；新增 HWND ownership marker、Edge 进程指纹、v2 状态安全恢复、旧状态安全退休、冷启动 nonce 窗口认领、登录后立即隐藏尝试、明确使用请求下的有限按需恢复，以及上游新窗口自然几何补丁。`npm.cmd test` 当前 29/29 通过，Node/PowerShell 静态解析通过。本轮没有执行真实桌面窗口、普通 Edge 并发、整页／元素截图或实际电脑重启登录验收；这些仍按本文验收清单等待用户安排。

## 一、目标与范围

用户遇到普通 Edge 新窗口从屏幕边缘出现的问题。已读取到普通窗口位置记忆曾为 `left=-486, right=30`；当前 BMG 把同一用户配置下的普通工作区窗口移到屏幕外，具备影响共享位置记忆的条件，但完整因果和修复效果仍需实测。

本次应统一处理：

1. 专用工作区采用 Windows 真正隐藏，保持已有 Edge 用户配置、Cookie 和登录状态。
2. 隐藏、后台操作、人工显示与再次隐藏组成完整流程。
3. 普通 Edge 窗口的位置、大小、焦点和最后关闭位置不被 BMG 后台维护干扰。
4. 电脑重启并登录后，计划任务能准备可用的隐藏工作区。
5. BMG 或 Edge 重启后能正确处理过期窗口记录，避免重复工作区及误操作普通窗口。
6. 普通后台截图明确指向工作区，能取得隐藏期间更新后的内容。

建议恢复策略为“登录时准备＋收到使用请求时恢复”。持续监控并自动拉起用户主动退出的 Edge 尚未获得用户确认；若认为需要该行为，先与用户讨论。窗口类型先保持现有普通 Edge 类型；若确有必要改变类型或用户配置隔离方式，先说明依据并确认。

## 二、已有证据及其边界

### 实施前基线（本计划制定时）

| 部件 | 当前行为 |
|---|---|
| `sidecar/workspace.mjs` | 创建不抢焦点的 480×360 工作区；保存 windowId、tabId、HWND 和显示状态；操作后维护隐藏状态 |
| `scripts/place-workspace-window-offscreen.ps1` | 将目标窗口移到 `(-32000, -32000)` 并设置工具窗口样式 |
| `scripts/show-workspace-window.ps1` | 查找可见目标 HWND，再恢复到前台并设置窗口位置与大小 |
| 两个窗口脚本 | 当前都用可见性筛选窗口，不能直接用于已经真正隐藏的窗口 |
| 空闲处理 | 默认 30 分钟后保留一个隐藏的空白标签页，清理该工作区其他标签页 |
| 普通后台截图 | `background: true`、`fullPage: false` 且无 selector 时，优先调用 CDP `Page.captureScreenshot` |
| 整页／元素截图 | 使用不同路径；当前扩展部分 `captureVisibleTab` 调用未指定窗口，不能推断其隐藏兼容性 |

### 已完成的独立试验

- 使用 `ShowWindowAsync(SW_HIDE)` 真正隐藏唯一新建测试窗口。
- `IsWindowVisible=false`，非最小化，隐藏前后坐标不变。
- 通过 BMG 上游 MCP 指定测试 tabId/windowId，普通后台截图成功。
- 隐藏期间导航到不同颜色和标题的新页面，内容读取与截图均更新。
- 第一轮隐藏中新页面截图与恢复可见后的截图文件完全一致。
- 第二轮隐藏后等待 10 秒再截图，完成后直接关闭；截图前后均不可见。
- 主窗口事件记录中，截图阶段没有显示事件；用户两轮均观察到短暂闪现。创建窗口到隐藏之间存在可见阶段，不能声称整个流程无闪现，也不能否定用户观察。
- 两轮测试标签均已关闭。

这些试验直接调用上游，绕过了正式 sidecar 操作后的屏幕外维护，因此证明的是“真正隐藏与普通后台截图可以配合”，不是正式 BMG 已经兼容。

复现入口：`scripts/test-hidden-window.mjs`、`scripts/probe-hidden-window.py`。本机证据位于 `.state/隐藏窗口试验/验证结论.md`，具体试验目录为 `BMG-HIDE-1789832483784` 和 `BMG-HIDE-1789832648399`。该目录被 Git 忽略；换机器接手时应重新试验，不能假定证据文件会随仓库分发。

尚未完成：长期隐藏、用户同时操作普通 Edge、整页／元素截图、正式工作区恢复、普通窗口位置记忆、真正的电脑重启验收。用户接受轻微短暂闪现，但没有要求忽略误截其他窗口、抢焦点或位置记忆污染。

### 本机实际计划任务

已只读核对 `BMG Sidecar`：

- 当前用户登录时触发，使用交互登录会话，普通权限；不是停留在 Windows 登录界面时就运行。
- 执行 `scripts/start.ps1 -EnsureWorkspace`。
- 失败时每分钟重试一次，最多三次；并发触发时忽略新实例。
- 工作区模式已启用；默认空闲时间 1800 秒。
- 启动脚本内部先检查工作区，失败后按 10、20、40 秒间隔重试。
- 检测不到当前会话的 Edge 进程时，才启动空白 Edge 窗口。
- 成功后计划任务退出，不持续监控服务或浏览器。

当前缺口：Edge 有进程不代表扩展可用；启动用空白窗口和正式工作区分开创建，缺少明确的归属及清理流程；sidecar 健康不代表浏览器工作区就绪。之前扩展曾停在“检测中／连接中”，用户重启后恢复，尚未定位原因，不能直接归因于计划任务。

## 三、预期生命周期

| 场景 | 预期行为 |
|---|---|
| 电脑重启并登录，Edge 尚未运行 | 启动 sidecar，启动能加载目标扩展的 Edge 配置，等待真实连接，再建立一个隐藏工作区 |
| 登录时 Edge 已运行且扩展已连接 | 复用连接，核对已有工作区，缺失时仅创建一次 |
| Edge 有后台进程但扩展未连接 | 报告浏览器未就绪，有限重试；不得把进程存在当成成功 |
| 同一浏览器会话中重启 sidecar | 验证并复用已有工作区，包括真正不可见的窗口 |
| 浏览器或电脑已经重启 | 重新确认工作区归属和窗口标识，不能直接沿用旧 HWND |
| 工作区被关闭 | 在下一次明确使用或启动准备时重建，不同时创建多个工作区 |
| 用户显示工作区进行登录 | 对同一个已确认工作区显示，保留网页、登录状态和标签页 |
| 人工处理完成 | 显式隐藏，或依现有规则在页面自动化恢复后隐藏 |
| 用户主动退出 Edge，当前没有使用请求 | 按建议方案不持续将 Edge 拉起；若需要相反行为，先确认 |
| 暂时断开连接或服务返回错误 | 区分通信失败与窗口确实不存在，避免丢弃记录后制造重复窗口 |

显示状态应区分“同一会话内人为显示”与“重启后的旧记录”：同一会话中重启 sidecar 不应意外打断正在进行的人工登录；登录冷启动默认准备隐藏工作区。实现时明确该规则并测试。

## 四、实施步骤

### 第一步：先完善窗口识别与真正隐藏

主要文件：`sidecar/workspace.mjs`、`sidecar/server.mjs`、两个窗口控制脚本。

- 把屏幕外移动替换为真正隐藏；可将隐藏脚本改为体现实际用途的名称，同步更新所有引用和工具说明。
- 隐藏操作应可重复调用：已隐藏时仍成功，验证不可见，不改坐标、不最小化、不抢焦点。
- 显示操作必须能找到不可见的窗口。只有明确人工显示才允许前台激活。
- 不以 `IsWindowVisible=false` 判定窗口不存在；在合适位置检查窗口句柄有效性、进程身份和工作区归属。
- 不能只凭旧 HWND 或“进程名是 msedge”判断归属。HWND、PID 和浏览器窗口 ID 都可能过期或复用；设计能关联本次浏览器会话与工作区的标记或映射，并覆盖复用场景。
- 页面导航会改变窗口标题，初次引导标题不能作为整个生命周期唯一凭据。
- 归属无法证明时，保留诊断信息并停止对该窗口的修改，不能任意关闭匹配到的标签页或把普通窗口当工作区。
- 保留一次性初始化合并逻辑，覆盖登录准备、浏览器请求和空闲清理的并发情况。

### 第二步：处理位置记忆与现有工作区迁移

- 隐藏方式变更并不自动解决位置记忆。分别观察创建、隐藏、显示、调整大小、关闭时 Edge 普通窗口的表现。
- 检查创建参数。现有 BMG 固定 480×360，上游 `chrome_navigate` 也有默认窗口尺寸；仅删除调用方宽高参数未必能消除覆盖。
- 不在 Edge 运行时直接改写整份 Preferences 文件；不要用固定坐标持续纠正用户普通窗口。
- 对已在屏幕外的旧工作区，只能在确认归属后迁移；不要把旧坐标作为未来人工显示位置。
- 确定是否能原地隐藏并恢复合理的工作区几何信息，或需要有序替换工作区。保留登录配置；涉及页面丢失的迁移应先说明。
- 如果现有普通窗口架构无法满足位置记忆目标，先用证据提出方案取舍，不擅自扩大为新的浏览器配置或窗口类型改造。

### 第三步：接入登录准备与按需恢复

主要文件：`scripts/start.ps1`、`scripts/ensure-workspace.mjs`、`scripts/ensure-edge.ps1`、`scripts/install-autostart.ps1`、本机工作区准备接口。

- 把服务可用、扩展连接可用、工作区已真正隐藏作为不同状态；启动成功必须达到工作区可用。
- 登录准备与普通使用共用工作区准备逻辑，避免各自创建窗口。
- 冷启动 Edge 时明确目标用户配置，保持用户既有登录状态。不要把任意同会话 Edge 进程视为扩展已就绪。
- 对用于加载扩展的临时窗口建立明确归属。优先复用为工作区；若需关闭，只关闭可证明由该次启动创建的窗口。
- 正确恢复服务重启后的 MCP 会话；不要凭会话初始化成功就声称浏览器操作可用。
- 保留有限重试、任务失败退出码和可定位的日志。服务已启动但扩展连接失败时，留下清晰状态，不无限创建空白窗口。
- 保持现有本机接口认证和访问范围；保持 sidecar、上游桥接与 Edge 的生命周期边界。
- 只在任务参数确实变化时重新注册计划任务；不要为验证而擅自重启电脑、结束所有 Edge 进程或扩大权限。

### 第四步：验证截图、闪现和普通 Edge 同时使用

- 普通后台截图使用明确 tabId/windowId，并保持已验证的 CDP 路径；不要为截图主动恢复或聚焦窗口。
- 从“窗口创建到隐藏”与“已隐藏窗口的连续截图”两个阶段分别记录时间、可见性、位置和显示事件。
- 复用同一隐藏窗口测试多个页面，不仅测试刚创建后的一张静态图；检查内容新鲜度、空白图、旧帧和尺寸变化。
- 与用户同时操作普通 Edge 的场景配合验证，检查目标页面、普通窗口焦点和位置；不能因为用户接受短暂闪现就忽略误截。
- 整页和元素截图必须独立检查。最低要求是明确目标窗口、避免误截其他窗口并如实返回能力限制；若需要额外扩展补丁，限制修改范围并说明。
- 若修改发行版扩展，补丁应保存在受 Git 管理的脚本中并接入安装流程，验证可重复应用。仅修改被忽略的 `extension/` 文件不算完成交付。

## 五、验收清单

| 验收项 | 需要的证据 |
|---|---|
| 隐藏幂等 | 连续隐藏同一工作区均成功；不可见、非最小化、坐标不变 |
| 隐藏窗口可找回 | 工具显示／隐藏和 sidecar 重启均能识别不可见工作区 |
| 目标身份可靠 | 模拟过期 HWND、进程重启及 ID 复用时，不操作普通窗口 |
| 页面操作正常 | 隐藏中导航、读取、交互及后续维护正确 |
| 普通截图正确 | 指定目标、内容更新、隐藏前后图片对照，无其他窗口内容 |
| 并发使用 | 用户操作普通 Edge 时，BMG 仍只操作并截图专用工作区 |
| 普通窗口位置记忆 | 普通窗口设定位置和大小并关闭；经过 BMG 创建、操作、维护后，新普通窗口仍符合用户期望 |
| 冷启动准备 | 分别验证 Edge 未运行、已有窗口、只有后台进程、扩展延迟连接 |
| 连接失败 | 有限重试且明确失败，服务状态与工作区状态可区分，无窗口堆积 |
| 去重与恢复 | 并发准备只产生一个工作区；sidecar 重启复用；Edge 重启后准确重建 |
| 启动临时窗口 | 不残留无归属的额外空白窗口，不关闭用户窗口 |
| 空闲处理 | 清理后仍保留一个真正隐藏的空白工作区，能再次使用 |
| 实际重启 | 用户安排电脑重启并登录后，确认工作区就绪和普通窗口行为；没有做则明确标注未验证 |

自动化测试优先覆盖生命周期、状态识别、失败重试和误操作防护；不要仅检查代码是否包含某个 API 名字。更新现有测试中对屏幕外实现的假设，运行 `npm.cmd test` 及适用的语法检查。真实窗口测试要记录其范围，不能以模拟测试代替实际重启验证。

## 六、交付与操作约束

- 原计划阶段要求在交接后由 5.6 sol 实施；该交接现已执行。后续若进行真实桌面或实际重启验收，仍需遵守下列操作约束。
- 用户已允许本次独立试验通过接口操作新建测试窗口；这不等于允许任意移动普通窗口或模拟鼠标键盘。后续实施按新的用户授权执行，桌面操作仍遵循全局规则。
- 若通过 API 修改工作区，目标必须具体可验证。需要用户重载扩展或实际重启时，明确说明操作及原因。
- 实现完成后更新中文 README，说明正常启动、连接失败、人工显示、旧状态迁移及验证结果。
- 按用户规则检查工作区与暂存区，只提交本任务纯文本修改，提交说明用中文；不提交 Cookie、凭据、日志、截图或会话文件。
- 最终报告分别说明：已实现、已实测、仅模拟验证、仍待用户配合。保留可复现的截图证据，对闪现如实说明。

## 七、交给 5.6 sol 的任务文本

> 请阅读本项目 PLAN.md，按“BMG 工作区真正隐藏与登录恢复实施计划”完成实现。先核对当前代码与已有试验证据，再处理窗口识别、真正隐藏、普通窗口位置记忆、登录准备和按需恢复。保持现有用户配置和登录状态；只操作可确认归属的 BMG 工作区。持续自动拉起用户主动退出的 Edge 等未确认行为，先讨论。完成有意义的自动化与真实窗口验证，更新 README 并按项目规则提交。实际重启及需要我操作的桌面步骤请明确交接；不要把未执行的验证写成通过。
